@{
    ViewBag.Title = "Camp list";
}

<h1>@ViewBag.Title</h1>

<div class="row">
    <div class="col-md-6">
        <h1>@ViewBag.Title</h1>
        <p>This will be a usefull site maybe...</p>

        <form>
            <div class="form-group">
                <label>Date</label>
                <input class="form-control" />
            </div>
            <div class="form-group">
                <label>Location</label>
                <input class="form-control" />
            </div>
            <div>
                <input type="submit" value="Add" class="btn btn-success" />
            </div>
        </form>
    </div>
    <div class="col-md-6">
        <h2>The Map</h2>
    </div>
</div>